# HTTP input
# http://localhost:8888/<tag>?json=<json>
<source>
  type http
  port 8888
</source>

## match not matched logs and write to file
<match **>
  type file
  path /tmp/fluent.phpunit
</match>
